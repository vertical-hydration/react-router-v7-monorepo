{
  "name": "@workspace/db",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "db:drop": "drizzle-kit drop",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:apply": "wrangler d1 migrations apply DB --local --config ../../apps/reserve/wrangler.jsonc --persist-to ../../.wrangler/state && wrangler d1 migrations apply DB --local --config ../../apps/reserve/wrangler.jsonc --persist-to ../../apps/reserve/.wrangler/state",
    "db:apply-prod": "wrangler d1 migrations apply DB --remote --config ../../apps/reserve/wrangler.jsonc",
    "db:apply-preview": "wrangler d1 migrations apply DB --remote --env preview --config ../../apps/reserve/wrangler.jsonc",
    "db:which-local": "sh ./scripts/db-which-local.sh",
    "db:studio": "sh ./scripts/db-studio.sh",
    "typecheck": "tsc --noEmit"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "better-auth": "^1.4.18",
    "better-sqlite3": "^12.6.2",
    "drizzle-orm": "~0.44.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.3",
    "drizzle-kit": "~0.31.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.2"
  }
}
